# Pushes the contents of the repo to the Codeberg mirror
name: 🪞 Mirror - Sync with Codeberg repo
on:
  schedule: [{ cron: '45 01 * * 0' }]
  push: { tags: ['v*'] }
  workflow_dispatch:

permissions:
  contents: read

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - uses: lissy93/repo-mirror-action@v1.6.0
        with:
          ssh_key: ${{ secrets.MIRROR_SSH }}
          host: git@codeberg.org
          user: alicia
          repo: email-comparison
